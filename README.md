Before running the notebook, one must clone the AthenaK repo from gitlab with the recursive flag:
```git clone --recursive https://TOKEN_NAME:TOKEN@gitlab.com/theias/hpc/jmstone/athena-parthenon/athenak.git```
